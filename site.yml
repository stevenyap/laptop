- hosts: all
  roles:
    - homebrew
    - rvm
    - themes
    - config
    - dotfiles
    - osx
    - ssh
  vars:
    machine_name: tao
    home_directory: /Users/stevenyap
    tmp_download_folder: ./tmp/download
